context / {
  type                    appserver
  location                /home/dope.ge/public_html/
  binPath                 /usr/bin/node
  appType                 node
  maxConns                100

  rewrite  {

  }
  addDefaultCharset       off
}
<VirtualHost *:80>

    ServerName dope.ge
    ServerAlias www.dope.ge
    ServerAdmin rexarkoexygen@gmail.com
    SuexecUserGroup dopeg5351 dopeg5351
    DocumentRoot /home/dope.ge/public_html
    CustomLog /home/dope.ge/logs/dope.ge.access_log combined
    AddHandler application/x-httpd-php80 .php .php7 .phtml
    <IfModule LiteSpeed>
        CacheRoot lscache
        CacheLookup on
    </IfModule>

 # Rewrite any http traffic to the main url https site
    <IfModule mod_rewrite.c>
        RewriteEngine On
        RewriteRule ^[^\/]*\/(.*) https://dope.ge/$1 [R=301,L]
    </IfModule>

</VirtualHost>

<VirtualHost *:443>

    ServerName dope.ge
    ServerAlias www.dope.ge
    ServerAdmin usman@cyberpersons.com
    SuexecUserGroup dopeg5351 dopeg5351
    DocumentRoot /home/dope.ge/public_html
    CustomLog /home/dope.ge/logs/dope.ge.access_log combined
    <IfModule LiteSpeed>
            CacheRoot lscache
            CacheLookup on
        </IfModule>
        SSLEngine on
    SSLVerifyClient none
    SSLCertificateFile /etc/letsencrypt/live/dope.ge/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/dope.ge/privkey.pem
    AddHandler application/x-httpd-php80 .php .php7 .phtml

# Basic security headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Xss-Protection "1; mode=block"

    # NodeBB header
    RequestHeader set X-Forwarded-Proto "https"

    # Static file cache
    <FilesMatch "\.(ico|jpg|jpeg|png|gif|js|css)$">
        <IfModule mod_expires.c>
            ExpiresActive on
            ExpiresDefault "access plus 14 days"
            Header set Cache-Control "public"
        </IfModule>
    </FilesMatch>

    ProxyRequests off
    <Proxy *>
        Order deny,allow
        Allow from all
    </Proxy>
</VirtualHost>

